Structured Query language is a [[declarative programming language]], which can be used to extract, manipulate, delete and insert data into a [[relational database]]. 
```
SELECT *
FROM table
WHERE constriction
```